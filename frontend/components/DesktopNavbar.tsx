import React from 'react'

const DesktopNavbar = ({ className }: {className: string}) => {
  return (
    <div className={`${className} `}>DesktopNavbar</div>
  )
}

export default DesktopNavbar